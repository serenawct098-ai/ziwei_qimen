# 紫微×奇門 SSOT 最終審計標準清單

## 1. 核心引擎與邏輯 (Core Engine & Logic)
- [ ] **庚干四化唯一真值**：必須為「太陽祿、武曲權、天同科、天相忌」（陽武同相）。
    - 檢查點：`traditional_core_engine_v2.1.json` 及其下游所有引用處。
    - 用戶修正：不保留舊版「陽武陰同」備份，直接刪除。
- [ ] **重忌特例邏輯**：必須包含「生年忌 + 自化忌」同星重疊的判定。
    - 檢查點：`21_zihua_self_transformation` 分支及解讀引擎 S7b。
- [ ] **辛干天魁鉞**：鎖定「六辛逢虎馬」預設（魁寅鉞午）。
- [ ] **早晚子時換日**：晚子時 (23:00-23:59) `lunar_day += 1`，時支恆為子。
- [ ] **體用分層**：南派為體、北派四化為用，權重 0.5:0.5，禁 12 宮互飛。

## 2. 數據層忠實度 (Data Layer Fidelity)
- [ ] **100% 原文忠實度**：嚴禁刪減、濃縮、白話改寫或意譯。
    - 檢查點：所有 `original_quote` 必須與原始 txt 檔案逐字對齊。
- [ ] **嚴禁錯誤精簡**：禁止對古籍原文進行任何形式的縮減、摘要或意譯。每個 `original_quote` 必須與原始文本字元序列完全一致。
- [ ] **嚴禁重複拆句**：除非原始文本本身存在重複，否則禁止在不同 JSON 檔案或同一檔案中對同一語句進行重複錄入。
- [ ] **逐句編號與溯源**：每個 entry 必須有唯一 `line_id` 及 `_source.line_ref`。
- [ ] **圖表處理**：所有圖表必須使用 `table_type` (matrix + original_block) 結構。
- [ ] **標籤治理**：後世格局（如馬頭帶劍、生不逢時）必須加 `_custom` 標籤與 `tag_reason`。

## 3. 系統純淨度 (System Purity)
- [ ] **無英文字符**：`original_quote` 中不得混入英文單詞（如 `not`）。
- [ ] **無冗餘文件**：GitHub 儲存庫中不得存在已刪除或損毀的舊 JSON 文件。
- [ ] **無腦補內容**：所有數據必須來自提供的 6 份原始古籍文件。

## 4. 交付格式 (Delivery Format)
- [ ] **JSON 結構對齊**：必須符合 `line_id`, `original_quote`, `tag`, `tag_reason` 標準格式。
- [ ] **審計日誌同步**：`audit_log_v1.json` 必須完整記錄所有任務執行狀態。
